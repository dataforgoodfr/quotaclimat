[tools]
opentofu   = "1.12.0"
terragrunt = "0.69.0"
# ansible installed separately (NixOS: use system package or nix shell)
# "pipx:ansible" = { version = "2.20.2", uvx = true, pipx_args = "--include-deps" }
uv = "latest"
