version: 2

sources:
  - name: advertising
    schema: advertising
    tables:
      - name: ad
      - name: ad_occurrence

  - name: public
    schema: public
    tables:
      # Sector / product category reference, loaded from the classification Google Sheet
      # before dbt runs (see my_dbt_project/external_sources.yml)
      - name: ad_sectors
        identifier: ref_ome_secteurs
      - name: ad_categories
        identifier: ref_ome_categories
      - name: program_metadata
