version: 2

sources:
  - name: advertising
    schema: advertising
    tables:
      - name: ad
      - name: ad_occurrence

  - name: public
    schema: public
    tables:
      # Sector / product category reference, loaded from a Google Sheet before dbt runs
      # (see my_dbt_project/external_sources.yml)
      - name: ad_classification
        identifier: ref_ad_classification
      - name: program_metadata
